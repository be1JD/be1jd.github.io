diff --git a/CSS/media_query.css b/CSS/media_query.css
index 6fc6ad9..b393275 100644
--- a/CSS/media_query.css
+++ b/CSS/media_query.css
@@ -29,7 +29,16 @@
 @media only screen and (max-width: 720px) {
     .quick_action_bar {
         top: calc(var(--nav-height) + 30px);
-        padding: 8px 12px;
+        padding: 8px 10px;
+        flex-wrap: wrap;
+        row-gap: 8px;
+    }
+
+    .quick_action_bar .back_btn {
+        flex: 0 0 auto;
+        min-height: 36px;
+        padding: 7px 10px;
+        font-size: 13px;
     }
 
     .quick_action_title {
@@ -39,16 +48,26 @@
     .quick_action_buttons {
         width: 100%;
         justify-content: stretch;
+        gap: 8px;
     }
 
     .quick_action_buttons .primary_btn,
     .quick_action_buttons .code_btn {
         flex: 1;
+        min-height: 40px;
+        min-width: 0;
+        padding: 8px 10px;
+        font-size: 13px;
+    }
+
+    .detail_tabs {
+        gap: 4px;
     }
 
     .detail_tab_btn {
-        font-size: 13px;
-        padding: 8px 12px;
+        font-size: 12.5px;
+        padding: 8px 10px;
+        min-height: 38px;
     }
 
     .site_header {
@@ -103,15 +122,19 @@
         font-size: clamp(34px, 11vw, 50px);
     }
 
-    .hero_stage img {
-        object-fit: contain;
-        object-position: center bottom;
+    .hero_stage {
+        display: none;
+    }
+
+    .hero {
+        min-height: auto;
+        padding-top: calc(var(--nav-height) + clamp(40px, 10vw, 64px));
+        padding-bottom: clamp(48px, 12vw, 80px);
+        background: linear-gradient(160deg, #141414 0%, #1c1c1c 55%, #0a0a0a 100%);
     }
 
-    .hero_stage::after {
-        background:
-            linear-gradient(180deg, rgba(5, 5, 5, 0.76) 0%, rgba(5, 5, 5, 0.58) 36%, rgba(5, 5, 5, 0.72) 100%),
-            linear-gradient(90deg, rgba(5, 5, 5, 0.48), rgba(5, 5, 5, 0.28));
+    .hero_copy {
+        width: 100%;
     }
 
     .project_gallery img,
